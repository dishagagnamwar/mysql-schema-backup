# mysql-schema-backup
Automated MySQL schema version control — dumps table, view, procedure, and trigger definitions via mysqldump on a schedule, detects changes, and auto-commits + pushes them to Git. Keeps a full structural history of your database without tracking actual data.
